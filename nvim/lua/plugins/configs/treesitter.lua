local options = {
  ensure_installed = { "lua", "php", "javascript", "typescript", "html", "css", "json", "yaml", "markdown", "bash", "python", "go" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
